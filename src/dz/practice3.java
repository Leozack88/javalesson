package dz;

public class practice3 {
    public static void main(String[] args) {
//4 типа операторов арифметические(плюс + / минус -  / умножение * / деление %  ,присваивания,сравнения, логические
        //int a=5;
        //int b=5; int b=10;  int b=a*2;
        //int b=a*2;

        // int x=1,y=2,z=3;  лучше записывать так , для упрощения через запятую. Только с переменными одного типа данных. к примеру int

        //int x=1;
        //int y=2;
        //int z=3;
        //int c=a+b;  так более читабельно чем если (a+b) отдельно.
        //System.out.println(a-b);


        // делние  дробная часть усекается если в переменной int
        //int d = 11 ;
        //int e = 3 ;
        //int f = d/e;


        //деление с дробной части
        //double d = 11 ;
        //double e = 3 ;
        //double f = d/e;

        //System.out.println(f);


        // что бы узнать остаток от деления в инт к примеру можно использовать оператор %

        //int d = 13 ;
        //int e = 3 ;
        //int celaya =d/e;
        //int ostatok=d%e;

        //System.out.println(celaya);
        //System.out.println(ostatok);

        // Инкремент (++) и декремент (--)  — это операции в Java, которые увеличивают или уменьшают значение переменной ровно на 1. оператор ++ -- называются Unary a=5 a++ =6       a -- = 4   ++ увеличивает на 1  -- уменьшает на 1 .
        // в зависимости от того в начале стоит или в конце ,по разному применяется.
        // поствикс это после ,(сначала идёт подсчет основных функций , затем уже увеличивается)
        // префикс это перед. (сначала увеличение или уменьешние в зависимости от ++ или --)
        //1. Префиксная форма (++x или --x)
        //Правило: «Сначала измени, потом используй».
        //
        //Java сначала увеличивает (или уменьшает) переменную, а затем использует её новое значение в текущем выражении.
        //2. Постфиксная форма (x++ или x--)
        //Правило: «Сначала используй, потом измени».
        //
        //Java сначала «запоминает» текущее значение переменной для использования в выражении, а уже после этого увеличивает (или уменьшает) саму переменную.
        //Где это имеет значение, а где — нет
        //Разница между префиксом и постфиксом критична только тогда, когда операция участвует в более сложном выражении (присваивание, математика, вызов метода).
        //
        //Когда разницы нет:
        //Если операция стоит отдельной строкой (сама по себе), то результат будет абсолютно одинаковым. Например, в классическом цикле for неважно, что вы напишете:
        //
        //Java
        //// Эти два варианта сработают абсолютно одинаково:
        //for (int i = 0; i < 5; i++) { ... }
        //for (int i = 0; i < 5; ++i) { ... }
        //Когда разница есть (Хитрый пример):
        //Ошибки часто возникают при выводе на экран или внутри математических расчетов.
        //
        //Java
        //int count = 10;
        //System.out.println(count++); // Выведет 10 (сначала использует, потом сделает 11)
        //System.out.println(count);   // Выведет 11
        //
        //int score = 10;
        //System.out.println(--score); // Выведет 9 (сначала отнимет, потом использует)
        //System.out.println(score);   // Выведет 9


        //int x=5;

        //int y=3;

        //   z = x - ++y;   // = 5-4
        //   z = x - y++;   // = 5-3

        //оператор присваивания   =
        //int a=3;
        //int b= 5;

        //  +=
        //int a=5;
        //a=a+3;  //(сначала (a+3)
        //a+=3;      // сначала прибавь , потом присвой.

        //-= /*= /    /=

       // int a=3;
        //int b=5;

        //a+=10;
        //a=a+10;
        //a++;

        //System.out.println(a);

        //b=b-2;
        //b-=2;

        //System.out.println(b);


        //b=b*2;
        //b*=2;

        //System.out.println(b);


        //b=b/2;
        //b/=2;

        //System.out.println(b);

        //int c=0;

        //a=b=c=18;    // оператор присвоения начинает работать с права
        //System.out.println(a);
        //System.out.println(b);
        //System.out.println(c);

        //присваивать переменным мы можем только то что в гарницах диапазонах

        //long a = 100L;

        //int b=10;

       // short s=b;
        // значение булеан
        int a=3;
        int b=5;
        int c=5;

// оператор ==  равенсвта // оператор больще или равно ,
      //  boolean b1 = a>=b;
       // boolean  b2=b<=5;
       // boolean b3 = a==b   // знак равенства двоеное ==    операттор раенства   / а знак одинарный  =  оператор  присвоения.

        // оператор неравенства !=
      // boolean b1 = a>=b;
       //boolean  b2=b<=5;
       //boolean b3 = c!=b

       // System.out.println(b3);

        // л\логические операторы && оператор and    Возвращает true, только если ОБА условия верны.      //
        // ||  ИЛИ (OR)  Возвращает true, если ХОТЯ БЫ ОДНО условие верно.



    }
}
